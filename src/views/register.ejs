<!DOCTYPE html>
<html lang="es">
    <class><%- include("partials/head") %>
    <title>Register PY Grupo 3</title>
    <link rel="stylesheet" href="css/styleRegister.css">
    <script src="/js/errorsForm.js" defer></script>
<body>
    <header>
        <%- include("partials/header") %>  
    </header>    
    <form action="/register" method="POST"  class="formulario"><!-- enctype="multipart/form-data" (agregarlo para que guarde la imagen pero al agregarlo deja de funcionar el form y toma todas las validaciones como erroneas)-->
        <h1 class="titulo-formulario">Registro</h1>
        <div class="control-formulario">
            <h5>Nombre</h5>
            <input type="text" name="nombre" placeholder="Ingrese su nombre" class="control" value="<%= oldData ? oldData.nombre : "" %>"/> <% if (errors && errors.nombre) { %>
            <p class="error-msg"><%= errors.nombre.msg %></p>
            <% } %>
        </div>
        <div class="control-formulario">
            <h5>Apellido</h5>
            <input type="text" name="apellido" placeholder="Ingrese su apellido" class="control" value="<%= oldData ? oldData.apellido : "" %>"/> <% if (errors && errors.apellido) { %>
            <p class="error-msg"><%= errors.apellido.msg %></p>
            <% } %>
        </div>
        <div class="control-formulario">
            <h5>Nombre de usuario</h5>
            <input type="text" name="usuario" placeholder="Ingrese su nombre de usuario" class="control" value="<%= oldData ? oldData.usuario : "" %>"/> <% if (errors && errors.usuario) { %>
            <p class="error-msg"><%= errors.usuario.msg %></p>
            <% } %>
        </div>
        <div class="control-formulario">
            <h5>Email</h5>
            <input type="email" name="email" placeholder="Ingrese su correo electrónico" class="control" value="<%= oldData ? oldData.email : "" %>"/> <% if (errors && errors.email) { %>
            <p class="error-msg"><%= errors.email.msg %></p>
            <% } %>
        </div>
        <div class="control-formulario">
            <h5>Contraseña</h5>
            <input type="password" name="contraseña" placeholder="Ingrese su contraseña" class="control" value="<%= oldData ? oldData.contraseña : "" %>"/> <% if (errors && errors.contraseña) { %>
            <p class="error-msg"><%= errors.contraseña.msg %></p>
            <% } %>
        </div>
        <div class="control-formulario">
            <h5>Confirme su contraseña</h5>
            <input type="password" name="contraseña2" placeholder="Confirme contraseña" class="control" value="<%= oldData ? oldData.contraseña2 : "" %>"/> <% if (errors && errors.contraseña2) { %>
            <p class="error-msg"><%= errors.contraseña2.msg %></p>
            <% } %>
        </div>
        <div class="control-formulario">
            <h5>Suba una imagen</h5>
            <input type="file" name="images" accept="images/*" class="control">
        </div>
        <button type="submit" class="boton-formulario">Registrarse</button>
        <button type="reset" class="boton-formulario2">Borrar</button>
        <div class="login">¿Ya tienes cuenta? <a href="/login">Iniciar sesión</a></div>
    </form>

    <footer class="footer">
        <%- include("partials/footer") %>
    </footer>
</body>
</html>